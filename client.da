import sys
from random import randint
olympus = import_da('olympus')
config = import_da('read_config_file')

client_properties = {}
clients = []
hosts   = []
olympus

class Client(process):
	def setup(timeout:int, configuration:set):
		print("client started")

	def run():
		output("executed")
		#send(('request',), to='name@host.')


	# def send_message_to_head():
 #        head = configuration['head']
	# 	send(('request',), to='name@host.')



def main():
	argv = sys.argv

	client = new(Client, num=1)
	setup(client, (3000,))
	print("client " + str(client))

	start(client.pop())
	#get_workload(client_properties['workload'])






# def read_properties():
# 	config.read_config_file()
# 	return config.get_client_properties()


# def pseudorandom(seed, number):
# 	random.seed(seed)
# 	operations = [randint(0,3) for index in range(0, number)]
# 	return operations


# def get_workload(workload):
# 	client_workload = []
# 	for index in range(0, len(workload)):
# 		if not workload[index].startswith('pseudorandom'):
# 			client_workload.append(workload[index])			
# 		else:
# 			client_workload.append(workload[index].split(';'))
# 	print("workload :: ")
# 	print(client_workload)